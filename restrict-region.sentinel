import "tfplan/v2" as tfplan

allowed_region = [
  "westus",
  "westus2",
  "centralus",
]

main = rule {
  all tfplan.resources.azurerm_resource_group as _, RG {
    all RG as _, r {
      r.applied.location in allowed_region
    }
  }
}
